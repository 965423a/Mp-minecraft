[package]
name = "mc-hotpath"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "mc_hotpath"
path = "src/lib.rs"

[dependencies]

[build-dependencies]
cc = { version = "1", features = ["parallel"] }